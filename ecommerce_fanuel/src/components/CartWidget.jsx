import React from 'react';
import CarritoCompras from '../img/carrito.svg';

const CartWidget = () => {
  return <img className='carrito' src={CarritoCompras} alt="Carrito" />;
};

export default CartWidget;
